# Dependencies & build
node_modules
dist
.cache
scripts/.font-cache
.next
.next-dev
coverage
tsconfig.tsbuildinfo
*.tsbuildinfo

# Env (never commit secrets)
.env
.env.local
.env.*.local

# User uploads (guest photos, vendor documents)
public/uploads

# IDE / OS
.aider*
*.iml

# Artefakty i notatki robocze (nie należą do repo, zostają lokalnie)
build-output*.txt
error.txt
*.jfif
test-invitation.pdf
zdj.png
weedingPlaner
Wedding AI Planner.pdf
test-app/
